# 引き継ぎメモ — 識別性改善の検討（2026-06-09）

別PCで作業を再開するための引き継ぎ文書。リポジトリ経由で共有するため `docs/` に置く。

## 0. 再開時の最初の一手（結論だけ先に）

**実機コーパス（`real_corpus.json` / `real_expected.yaml`）の拡充、特に out-of-scope を 5 → 最低20件。**
これが揃うまで識別アルゴリズム（棄却・スコア正規化など）には手を付けない。理由は §3。

---

## 1. リポジトリの状態

- `main` は GitHub と同期済み、最新 `7f2bb08 (#7)`。作業ツリーはクリーン。
- オープンな Issue / PR は 0 件。git 上に「残った宿題」は無い。
- DB は 47 回路まで拡充済み（README 記載の31は古い）。

## 2. 今日やったこと＝「次の一手」の検討

「DB拡張をしたい」という発想から出発し、評価を実測して方向性を検討した。

### 実測値（2026-06-09 時点）

| 指標 | 結果 | 解釈 |
|---|---|---|
| 自己検索 Hit@1/@3（`evaluate.py`） | 100% / 100% | DB内部は飽和。**伸びしろゼロ** |
| 実機 Hit@1 strict（`validate_real.py`） | 80.0% [CI 54.8–93.0] | 自己検索との差＝汎化ギャップ |
| 実機 Hit@3 | 93.3% | 正解は上位には来る |
| **in/out 分離余裕** | **−0.18** | 棄却の核心的弱点 |
| 棄却 top1 単独 AUC（`reject_eval.py`） | 0.867（naive） | **ネストCV bacc は 0.600**（+0.23の楽観バイアス） |

### 判明した衝突の中身

| 回路 | 種別 | top1 | 備考 |
|---|---|---|---|
| `generic_opamp_bip` | **out** | **0.785** | OpAmpマクロモデル。MFBバンドパスに構造が似て高得点（誤受理） |
| `royer1` | out | 0.722 | Royer発振器。ブリッジ整流に化ける |
| `buck_conv` | **in（正解）** | 0.710 | 正しくBuck判定なのに低い |
| `amplifier-ac` | **in（正解）** | 0.671 | NPN×3多段。正解なのに最低 |

「正解なのに0.67、誤りなのに0.785」が −0.18 の正体。

## 3. 重要：診断の訂正（途中で1つ誤りを発見）

当初「受動0.90 vs 増幅0.67 ＝ ファミリ間スケール差」と診断し、**ファミリ相対スコア正規化**を提案した。
→ **これは誤り。撤回した。**

`circuit_rag.py:306` のスコア式 `alpha*topo + (1-alpha)*tag` と `validate_real.py`（alpha=0.9・タグ無し）から、
表示スコア = `0.9 × topo`。逆算すると：

| 回路 | 表示 | topo | DB厳密一致 |
|---|---|---|---|
| RC-Lowpass | 0.9000 | 1.000 | あり |
| NPN-Amplifier-CE | 0.8565 | 0.951 | **あり** |
| amplifier-ac | 0.671 | 0.746 | **なし**（NPN×3多段が未収録） |
| buck_conv | 0.710 | 0.789 | **なし**（簡略制御Buck） |

- **0.9000 は alpha=0.9 の天井アーティファクト**であって、ファミリ特性ではない。複数ファミリが一律 0.9 に張り付く。
- 厳密一致がある能動回路（NPN-CE/NMOS-CS）は topo 0.95 まで出る。
- **in-scope が低得点になる真因＝「DBに最近傍が無い（近傍距離が遠い）」**。ファミリのスケールではない。

### この訂正の含意

1. **ファミリ相対正規化を実装しても直らない**（前提が崩れた）。
2. **低得点 in-scope に対応するトポロジをDBに足せば top1 が上がり、分離余裕が改善する** → 当初の「DB拡張」案は in-scope の near-miss に対しては**部分的に正しかった**。
3. ただし out-scope の誤受理（`generic_opamp_bip` 等）は構造的に紛らわしく、DB拡張・閾値・特徴では**クリーンに切れない**。これは「何を in-scope と定義するか」という**スコープ定義の問題**。

### さらに本質的な制約：そもそも測れていない

- out-of-scope が **5件だけ** → TNR の95%CI [0.38, 0.96]、AUC CI [0.36, 0.88]：**無情報**。
- 分離余裕 −0.18 は in側1〜2点・out側1〜2点で決まる。`generic_opamp_bip` 1点を抜けば符号が反転する程度の脆さ。
- ネストCVバイアス +0.23 は、この極小データへの**過適合の症状**。
- → **今の n=20 で識別アルゴリズムを直しても、改善か偶然か判定不能。**「測定基盤を直す」が論理的に先。

> 反省：4点のデータに「ファミリスケール」という物語を後付けし、解法（正規化）まで作ってしまった。
> 再監査で否定された。同じ轍を踏まないこと＝**ノイズに過適合した解を作らない**。

## 4. 実機コーパス拡充のやり方

### 構成
- `real_corpus.json` … 第三者が描いた回路（TopoRAG形式）。**全クエリ タグ無し**（汎化テストのため）。
- `real_expected.yaml` … `scope: in/out` + `expect: [DB_id,...]` + `note`。
- 出典：`feastorg/KiCad-Simulation-Examples` と KiCad 同梱 `demos/simulation`。

### 統計的な目標
| n（各クラス） | 比率CI幅 | 判定 |
|---|---|---|
| 5 | ±0.3〜 | 無情報（現状の out） |
| 30 | ±0.15 | 実用最低ライン |
| 50 | ±0.1 | 安心 |

当面：**out 5→最低20（できれば30）、in 15→30。**

### 1件追加する手順
```powershell
# 1. 第三者の .kicad_sch を入手（kicad_real_test/ 等。READMEで git 追跡除外済み）
python kicad_sch_to_toporag.py path\to\NewCircuit.kicad_sch new.json   # 2. 変換
# 3. new.json の中身を real_corpus.json の circuits に追記
# 4. real_expected.yaml にラベル追加（scope / expect / note）
python validate_real.py   # 5. 再評価
python reject_eval.py
```

### 設計上の落とし穴（必読）
- **独立性**：DBを見てから回路を選ばない（自己検索化を避ける）。
- **ブラインドラベリング**：**スコアを見る前に scope を決める**。「低く出たから out」は循環論法で棄却性能を偽装する。既存コーパスに後付けラベルが混じっていないかも要点検。
- **out は多様＆境界ケース優先**：最も価値が高いのは `generic_opamp_bip` のような「構造的に in と紛らわしい out」。境界を定義するのはこれら。
- **in/out バランス**：AUCは両クラス数に効く。片方だけ増やさない。
- **easy/hard を分けて集計**：厳密一致 in（opamp-inverting 等）は Hit@1 を水増しする。hard in（amplifier-ac, smps-com）と分離して報告できると良い。

## 5. 再開時のタスク（順序）

1. **棚卸し**：feastorg / KiCad demos で**未取り込みの回路を全部リスト化**。手元に元 `.kicad_sch` がどれだけ残っているか確認（拡充の初速を決める）。
2. **out-scope を優先 +10〜15**（多様な未知系統＋境界ケース）。
3. **in-scope を near-miss狙いで +10**（DBに最近傍が無いタイプを意図的に）。
4. 各回路を**スコア確認前にブラインドでラベリング**。
5. 再評価。CI幅が締まって**初めて**識別ロジック（棄却・スコア処理）の是非を判断する。

## 6. やってはいけないこと（袋小路）
- ファミリ相対スコア正規化の実装（§3で前提が否定された）。
- margin / ratio_gap の融合（`reject_eval.py` で AUC 0.62〜0.63、融合は 0.773 で**むしろ悪化**済み）。
- n=20 のまま閾値・特徴をチューニングして「改善した」と判断すること（ノイズへの過適合）。

## 7. 追補（2026-06-09 レビュー）— 計画の3つの穴

§5 の計画を再点検して見つかった、データ収集だけでは塞がらない穴。着手前に潰すこと。

### 7.1 スコープ定義を DB から独立させる（最優先・§3.3 の本丸）
現行の scope 定義は `real_expected.yaml` 冒頭で **「in = DB にカテゴリが存在」** という *DB 相対* の定義になっている。これは §4 のブラインドラベリングと矛盾する：
- DB を変えるたびに scope ラベルが動く。実際 `sallen_key` は `sallen_key_lpf_001` を DB に足したことで **out→in** に変わった（`real_expected.yaml:73-77`）。
- 「スコアを見る前に scope を決める」ためには、DB の中身に依存しない **in-scope の分類体系（何を1つの機能カテゴリと認めるか）** が論理的に先に要る。
- → **§5 のタスク0 として「DB から独立した in-scope 定義を明文化」を追加**。これが無いと out を20件集めても境界はぼやけたまま（§3.3 の「スコープ定義の問題」は集めても解けない）。

### 7.2 データ枯渇時の分岐を先に決める
out 5→20（できれば30）・in 15→30 を feastorg + KiCad demos の **2ソースだけで賄えるかは未検証**。ここが枯れると「測定基盤を直す」ゲートが無期限に詰まる。§5.1 の棚卸し結果を**最初に**見て、20件に届かない場合の分岐を先に用意する：
- (a) ソース追加（LTspice デモ、教科書回路など）。
- (b) 合成ハード負例：in-scope 回路をカテゴリが崩壊するまで摂動して out を作る。out はラベルが構築時に確定するためブラインド原則と両立する（in には使えない）。ただし実分布とのズレに注意し、実機 out とは集計を分ける。

### 7.3 棄却の「撤退基準」を事前登録する
§3 の「n が小さく改善か偶然か判別不能」という前提からは、**「棄却は構造的に不可能」とは結論できない**（無情報なデータから不可能は導けない）。代わりに撤退基準を *スコアを見る前に* 決めておく：
- **拡充後（n_out≧20）でもなお棄却 AUC の 95%CI が 0.5 を跨ぎ、ネスト CV bacc がチャンス水準なら、硬い受理/棄却の二値判定を捨て、ランカー（top-3＋スコア提示、受理判断は人）に製品定義を切り替える。**
- これは「今諦める」ではなく、データ集めが届かない目標を無限に追うのを防ぐストッピングルール。§6 の反過適合・ブラインド哲学と整合する。

> 補足：§3.2 の「低得点 in-scope のトポロジを DB に足せば top1 が上がる」は DB 操作であって §5 のコーパス拡充とは別物。ただし**テストコーパスを見て DB を足すと自己検索化**する（§4 独立性）。DB 拡充と評価コーパス拡充は独立に回し、相互汚染させないこと。

### 7.4 実装状況（2026-06-09 反映）
§7.1 と §7.3 はコードに落とし込み済み（検索アルゴリズムは未変更＝§0 の方針通り）。§7.2 は外部 `.kicad_sch` 入手が前提のため本メモへの方針記載のみ。

- **§7.1（DB 非依存スコープ定義）→ 実装済み**
  - `scope_taxonomy.yaml`（新規）: in-scope を 7 family の機能分類として DB 非依存に定義。
  - `real_expected.yaml`: 全 in エントリに `family:` を付与。scope は family から導く（`sallen_key` の根拠を「DB に追加したから」→「family=filter_active に該当」へ修正）。
  - `check_scope.py`（新規）: `python check_scope.py` で family と scope の整合・DB 相対根拠の混入・コーパス未ラベルを検査（CI 用に exit code を返す）。現状 ✓ 整合。
  - **拡充時の運用**: 新規 out を足す前に「どの family の defining_criteria も満たさない」ことを確認。新カテゴリを in にしたければ先に taxonomy へ family を追加してから DB・コーパスを足す（DB を足してから後付けで in にしない）。
- **§7.3（撤退基準の事前登録）→ 実装済み**
  - `reject_eval.py`: 事前登録パラメータ `GATE_MIN_N_OUT=20 / GATE_AUC_CI_LO=0.50 / GATE_NESTED_BACC=0.65` と `rejection_gate_verdict()`。実行末尾に判定を表示。
  - 現状の判定: **INSUFFICIENT（n_out=5 < 20）** ＝ゲート未到達。コーパス拡充を継続し識別ロジックは保留。
  - 参考: 現データの最良シグナル top1 は AUC CI 下限 0.653 だが **nested bacc 0.600 < 0.65**。仮に n_out≥20 でこの水準のままなら判定は **RETREAT_TO_RANKER**（硬い棄却を捨ててランカー化）になる。拡充で nested bacc が 0.65 を超えるかが分水嶺。

## 関連ファイル
- 評価：`evaluate.py` / `validate_real.py` / `reject_eval.py` / `ablation.py`
- コーパス：`real_corpus.json` / `real_expected.yaml`
- 棄却実装：`circuit_rag.py` の `search_with_rejection`（`PROVISIONAL_REJECT_THRESHOLD`＝未校正の暫定値）
- 変換：`kicad_sch_to_toporag.py`
- 設計：`docs/EVALUATION_DESIGN.md`
